#include <bits/stdc++.h>
using namespace std;

int main()
{
    short n, m;
    cin >> n;
    int A[n];
    for (int i = 0; i < n; i++)
    {
        cin >> A[i];
    }
    cin >> m;
    int B[m];
    for (int i = 0; i < m; i++)
    {
        cin >> B[i];
    }
    for (int i = 0; i < n; i++)
    {
        /* code */
    }
    

    

    return 0;
}