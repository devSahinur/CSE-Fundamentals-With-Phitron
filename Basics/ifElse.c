#include <stdio.h>
#include <stdbool.h>

int main()
{
    bool timer = 10 > 5; 
    
    if(timer)
    {
        printf("Timer is True");
    }
    else{
        printf("Timer is False"); 
    }

    return 0;
}
